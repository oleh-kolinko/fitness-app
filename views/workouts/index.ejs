<h2>All workouts:</h2>

  <% workouts.forEach( workout =>{ %>

    <a href="workouts/<%= workout._id %>">
    <div class='card-panel'>
      
        <%= workout.name %>
    </div>
  </a>

  <% }) %>
